# RiskCalc runtime + test dependencies
# Requires Python >= 3.10 (PEP 604 unions in type annotations at runtime).
# Floors chosen for compatibility; newer versions are fine.
numpy>=1.24
scipy>=1.10
PySide6>=6.5
matplotlib>=3.7

# Testing
pytest>=7.0
